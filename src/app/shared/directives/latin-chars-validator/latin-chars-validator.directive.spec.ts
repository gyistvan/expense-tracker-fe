import { LatinCharsValidatorDirective } from './latin-chars-validator.directive';

describe('LatinCharsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LatinCharsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
